spring:
  application:
    name: daily-driver

  datasource:
    url: jdbc:postgresql://localhost:5432/daily_driver
    username: postgres
    password: admin123
    driver-class-name: org.postgresql.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate # update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 1110
